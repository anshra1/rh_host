part of '../../import.dart';

class BatchPage extends StatelessWidget {
  const BatchPage({super.key});

  static const String routeName = '/batch-page';

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
