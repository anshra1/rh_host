part of '../../../batch/import.dart';

class ServicesPage extends StatelessWidget {
  const ServicesPage({super.key});
  static const String routeName = '/sevices-page';
  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
