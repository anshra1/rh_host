abstract class DeviceTokens {
  // Screen densities
  static const double ldpi = 0.75;
  static const double mdpi = 1;
  static const double hdpi = 1.5;
  static const double xhdpi = 2;
  static const double xxhdpi = 3;

  // Notch/Safe area handling
  // static EdgeInsets get deviceSafeArea => EdgeInsets.only(
  //   top: MediaQuery.of(navigatorKey.currentContext!).viewPadding.top,
  //   bottom: MediaQuery.of(navigatorKey.currentContext!).viewPadding.bottom,
  // );

  // Device orientation specific
//   static bool get isLandscape =>
//     MediaQuery.of(navigatorKey.currentContext!).orientation ==
//     Orientation.landscape;
}
